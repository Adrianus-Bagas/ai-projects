[project]
name = "api"
version = "0.1.0"

dependencies = [
    "fastapi",
    "uvicorn",
    "alembic",
    "database",
    "auth"
]

[tool.uv.sources]
database = { path = "../../packages/database", editable = true }
auth = { path = "../../packages/auth", editable = true }
