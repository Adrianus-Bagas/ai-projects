[project]
name = "auth"
version = "0.1.0"
description = "Reusable authentication utilities for the AI Projects monorepo"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["auth"]